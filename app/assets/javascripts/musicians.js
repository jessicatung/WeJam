// This seems like it should go in a file called initialize.js or something
// else.  I'm expecting here a model that wraps an array of instances of
// Musician.
$(document).ready(function(){
	// whole app is running here.
	WeJam.initialize()
})

